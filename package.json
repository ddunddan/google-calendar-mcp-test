{
  "name": "google-calendar-mcp-test",
  "version": "1.0.0",
  "description": "Google Calendar integration with MCP (Model Code Protocol) test",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node build/index.js",
    "auth": "npm run build && node build/auth-server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "google-auth-library": "^9.0.0",
    "googleapis": "^128.0.0",
    "open": "^10.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}